"""兼容旧导入路径 `app.agent.proactive_care`。

主动看屏已由 ProactiveObserver 接管；本模块只做别名转发。
新代码请用 `app.agent.screen_awareness`（开关）与 `app.perception.observer`（运行时）。
"""

from __future__ import annotations

from app.agent.screen_awareness import (
    SCREEN_AWARENESS_CONTEXT_HISTORY_MARKER,
    SCREEN_AWARENESS_DEFAULT_CHECK_INTERVAL_MINUTES,
    SCREEN_AWARENESS_DEFAULT_COOLDOWN_MINUTES,
    SCREEN_AWARENESS_DEFAULT_SCREEN_CONTEXT_BATCH_LIMIT,
    SCREEN_AWARENESS_MAX_CHECK_INTERVAL_MINUTES,
    SCREEN_AWARENESS_MAX_COOLDOWN_MINUTES,
    SCREEN_AWARENESS_MAX_SCREEN_CONTEXT_BATCH_LIMIT,
    SCREEN_AWARENESS_MIN_CHECK_INTERVAL_MINUTES,
    SCREEN_AWARENESS_MIN_COOLDOWN_MINUTES,
    SCREEN_AWARENESS_MIN_SCREEN_CONTEXT_BATCH_LIMIT,
    SCREEN_AWARENESS_TIMER_DUE_GRACE_SECONDS,
    SCREEN_AWARENESS_TIMER_POLL_INTERVAL_MS,
    ScreenAwarenessSettings,
)

# 以下别名仅兼容旧导入；数值本身属于已停用的 ScreenAwareness 批次定时路径。
PROACTIVE_DEFAULT_CHECK_INTERVAL_MINUTES = SCREEN_AWARENESS_DEFAULT_CHECK_INTERVAL_MINUTES
PROACTIVE_DEFAULT_COOLDOWN_MINUTES = SCREEN_AWARENESS_DEFAULT_COOLDOWN_MINUTES
PROACTIVE_DEFAULT_SCREEN_CONTEXT_BATCH_LIMIT = SCREEN_AWARENESS_DEFAULT_SCREEN_CONTEXT_BATCH_LIMIT
PROACTIVE_MIN_CHECK_INTERVAL_MINUTES = SCREEN_AWARENESS_MIN_CHECK_INTERVAL_MINUTES
PROACTIVE_MAX_CHECK_INTERVAL_MINUTES = SCREEN_AWARENESS_MAX_CHECK_INTERVAL_MINUTES
PROACTIVE_MIN_COOLDOWN_MINUTES = SCREEN_AWARENESS_MIN_COOLDOWN_MINUTES
PROACTIVE_MAX_COOLDOWN_MINUTES = SCREEN_AWARENESS_MAX_COOLDOWN_MINUTES
PROACTIVE_MIN_SCREEN_CONTEXT_BATCH_LIMIT = SCREEN_AWARENESS_MIN_SCREEN_CONTEXT_BATCH_LIMIT
PROACTIVE_MAX_SCREEN_CONTEXT_BATCH_LIMIT = SCREEN_AWARENESS_MAX_SCREEN_CONTEXT_BATCH_LIMIT
PROACTIVE_TIMER_POLL_INTERVAL_MS = SCREEN_AWARENESS_TIMER_POLL_INTERVAL_MS
PROACTIVE_TIMER_DUE_GRACE_SECONDS = SCREEN_AWARENESS_TIMER_DUE_GRACE_SECONDS
PROACTIVE_SCREEN_CONTEXT_HISTORY_MARKER = SCREEN_AWARENESS_CONTEXT_HISTORY_MARKER

ProactiveCareSettings = ScreenAwarenessSettings
