# Cursor task

Implement `docs/superpowers/plans/2026-08-26-observer-plain-dialogue-fallback.md` strictly with TDD.

Use model `cursor-grok-4.6-high-fast`. First inspect the current parser and nearby tests. Record RED evidence before production edits. Keep the classifier conservative and pure. Do not alter prompts, API request payloads, retries, translation, cooldowns, persona files, settings, or UI. Do not commit or push.

Write the exact changed files, RED evidence, focused/full test results, `git diff --check`, and remaining risks into the Cursor section of `integration-notes.md`.
